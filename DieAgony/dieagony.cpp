#include <unordered_set>
#include <vector>
using namespace std;

void dfs(vector<vector<int>>& grid, 
         int row, 
         int col,
         int score,
         int turn,
         unordered_set<int>& die_faces) {
    // Base case: You have arrived at 
}

int main() {
    // Create the grid

    // Initialise the map for the die_faces
}